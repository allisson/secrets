# Database And Migrations

- PostgreSQL migrations live in `migrations`.
- Add matching `up` and `down` migration scripts.
- Use `.env.example` as the local configuration template.
- Do not commit real `.env` secrets.
