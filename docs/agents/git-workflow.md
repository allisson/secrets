# Git Workflow

- Use Conventional Commits.
- Common scopes include `auth`, `tokenization`, and `release`.
- Keep commit subjects imperative and concise.
- Pull requests need a summary, validation steps, and a linked issue when relevant.
